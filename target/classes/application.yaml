server:
  port: 8080
spring:
  application:
    name: shop-page
shop:
  rest:
    url: lb://shop-rest
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
